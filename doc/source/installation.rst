============
Installation
============

At the command line::

    $ pip install github-webhook-handler

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv github-webhook-handler
    $ pip install github-webhook-handler
